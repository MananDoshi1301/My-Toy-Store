import React from 'react'
import { projectStorage, projectFirestore, timestamp } from "../../Firebase/config";

const AddProductDB = ({ id, resetAdded }) => {

  return (
    <div>

    </div>
  )
}

export default AddProductDB
